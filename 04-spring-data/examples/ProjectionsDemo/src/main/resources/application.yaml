spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:mem:projdemodb;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: WARN
    com.example.projections: INFO
